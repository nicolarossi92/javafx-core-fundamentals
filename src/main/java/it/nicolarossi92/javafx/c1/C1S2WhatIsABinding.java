package it.nicolarossi92.javafx.c1;

/**
 * Data binding defines a relation between data elements in a program to keep them synchronized. In a GUI
 * application, data binding is frequently used to synchronize the elements in the  data model with the corresponding UI elements.
 * For a binding to work correctly, it is necessary that the binding is notified whenever its dependencies change. When dependencies become invalid or they change, all listeners are notified.
 * A binding can be eager or lazy. In a eager one, the bound variable is recomputed immediately after its dependencies change. In
 * a lazy one, the bound variable is not recomputed when its dependencies change. Rather, it is recomputed when it is read the next time.
 * A binding can be unidirectional or bidirectional. Unidirectional works only in one direction, whereas bidirectional works both ways.
 *
 *
 * Applications with GUIs provide users with UI widgets, for example, text fields. The data displayed in UI widgets have to be synchronized with the underlying data model and vice versa.
 */
public class C1S2WhatIsABinding {
}
